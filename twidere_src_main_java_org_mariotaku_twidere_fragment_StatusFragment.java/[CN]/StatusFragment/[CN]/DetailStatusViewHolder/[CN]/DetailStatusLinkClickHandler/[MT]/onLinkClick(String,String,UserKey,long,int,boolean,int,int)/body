{
  final ParcelableStatus status=adapter.getStatus();
  ParcelableMedia current;
  if ((current=ParcelableMediaUtils.findByUrl(status.media,link)) != null && !current.open_browser) {
    expandOrOpenMedia(current);
    return true;
  }
  if ((current=ParcelableMediaUtils.findByUrl(status.quoted_media,link)) != null && !current.open_browser) {
    expandOrOpenMedia(current);
    return true;
  }
  return super.onLinkClick(link,orig,accountKey,extraId,type,sensitive,start,end);
}
