{
  final ParcelableStatus status=mStatusAdapter.getStatus(position);
  AbsStatusesFragment.handleStatusActionClick(getContext(),getFragmentManager(),mTwitterWrapper,(StatusViewHolder)holder,status,id);
}
