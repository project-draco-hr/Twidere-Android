{
  for (  ParcelableMedia mediaItem : media) {
    if (mediaItem.type == ParcelableMedia.Type.TYPE_VIDEO || mediaItem.type == ParcelableMedia.Type.TYPE_ANIMATED_GIF)     return true;
  }
  return false;
}
