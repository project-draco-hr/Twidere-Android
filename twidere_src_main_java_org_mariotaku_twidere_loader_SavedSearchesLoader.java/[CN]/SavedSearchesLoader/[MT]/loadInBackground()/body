{
  final MicroBlog twitter=MicroBlogAPIFactory.getTwitterInstance(getContext(),mAccountId,false);
  if (twitter == null)   return null;
  try {
    return twitter.getSavedSearches();
  }
 catch (  final TwitterException e) {
    Log.w(LOGTAG,e);
  }
  return null;
}
