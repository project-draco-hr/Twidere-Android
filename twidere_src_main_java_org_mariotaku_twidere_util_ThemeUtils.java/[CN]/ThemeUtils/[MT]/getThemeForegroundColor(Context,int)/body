{
  @SuppressWarnings("ConstantConditions") final TypedArray a=context.obtainStyledAttributes(null,new int[]{android.R.attr.colorForeground},0,theme);
  try {
    return a.getColor(0,0);
  }
  finally {
    a.recycle();
  }
}
