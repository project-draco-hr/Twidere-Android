{
  final TypedArray a=context.obtainStyledAttributes(new int[]{R.attr.windowNormalBackground,android.R.attr.windowBackground});
  try {
    if (a.hasValue(0)) {
      return a.getDrawable(0);
    }
 else {
      return a.getDrawable(1);
    }
  }
  finally {
    a.recycle();
  }
}
