{
  boolean containsFile=false;
  if (null == params)   return false;
  for (  final HttpParameter param : params) {
    if (param.isFile()) {
      containsFile=true;
      break;
    }
  }
  return containsFile;
}
