{
  addProcessingFriendshipRequestId(mAccountKey,mUserId);
  final FollowRequestTaskEvent event=new FollowRequestTaskEvent(FollowRequestTaskEvent.Action.ACCEPT,mAccountKey,mUserId);
  event.setFinished(false);
  bus.post(event);
  super.onPreExecute();
}
