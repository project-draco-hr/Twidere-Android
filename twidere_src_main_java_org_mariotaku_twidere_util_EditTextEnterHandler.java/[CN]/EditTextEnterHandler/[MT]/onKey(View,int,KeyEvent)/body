{
  if (keyCode == KeyEvent.KEYCODE_ENTER && enabled && event.getAction() == KeyEvent.ACTION_DOWN) {
    if (listener != null) {
      listener.onHitEnter();
    }
    return true;
  }
  return false;
}
