{
switch (linkId) {
case LINK_ID_USER:
{
      break;
    }
case LINK_ID_SEARCH:
{
    ThemeUtils.applyActionBarBackground(actionBar,this,getCurrentThemeResourceId(),getCurrentThemeColor(),false);
    break;
  }
default :
{
  ThemeUtils.applyActionBarBackground(actionBar,this,getCurrentThemeResourceId(),getCurrentThemeColor(),true);
  break;
}
}
}
