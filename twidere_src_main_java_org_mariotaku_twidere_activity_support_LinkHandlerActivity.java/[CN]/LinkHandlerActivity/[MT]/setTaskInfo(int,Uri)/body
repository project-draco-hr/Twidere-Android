{
switch (linkId) {
default :
{
      if (ThemeUtils.isColoredActionBar(getCurrentThemeResourceId())) {
        ActivityAccessor.setTaskDescription(this,new TaskDescriptionCompat(null,null,getCurrentThemeColor()));
      }
      break;
    }
}
}
