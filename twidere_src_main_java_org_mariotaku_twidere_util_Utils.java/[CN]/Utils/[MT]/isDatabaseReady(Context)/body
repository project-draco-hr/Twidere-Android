{
  final Cursor c=context.getContentResolver().query(TwidereDataStore.CONTENT_URI_DATABASE_READY,null,null,null,null);
  try {
    return c != null;
  }
  finally {
    if (c != null) {
      c.close();
    }
  }
}
