{
  updateTitle(mode);
  final FragmentActivity activity=getActivity();
  if (activity instanceof IThemedActivity) {
    ThemeUtils.applySupportActionModeItemColor(mode,((IThemedActivity)activity).getCurrentThemeColor());
  }
  return true;
}
