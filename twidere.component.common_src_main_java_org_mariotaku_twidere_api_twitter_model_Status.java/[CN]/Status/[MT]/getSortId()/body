{
  if (sortId != -1)   return sortId;
  sortId=rawId;
  if (sortId == -1) {
    sortId=Long.parseLong(id);
  }
  if (sortId == -1 && createdAt != null) {
    sortId=createdAt.getTime();
  }
  return sortId;
}
