{
  final FragmentActivity activity=getActivity();
  if (activity == null)   return;
  if (mRecipient != null) {
    activity.setTitle(userColorNameManager.getDisplayName(mRecipient,preferences.getBoolean(KEY_NAME_FIRST)));
  }
 else {
    activity.setTitle(R.string.direct_messages);
  }
}
