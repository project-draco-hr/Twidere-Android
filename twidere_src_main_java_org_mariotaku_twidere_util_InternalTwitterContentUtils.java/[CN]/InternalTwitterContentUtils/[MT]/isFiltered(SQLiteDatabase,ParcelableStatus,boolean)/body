{
  if (database == null || status == null)   return false;
  return isFiltered(database,status.user_key,status.text_plain,status.text_html,status.source,status.retweeted_by_user_id,status.quoted_user_id,filter_rts);
}
