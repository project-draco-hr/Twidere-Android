{
  if (KEY_NICKNAME_ONLY.equals(key) || KEY_DISPLAY_PROFILE_IMAGE.equals(key) || KEY_MEDIA_PREVIEW_STYLE.equals(key)|| KEY_DISPLAY_SENSITIVE_CONTENTS.equals(key)) {
    notifyDataSetChanged();
  }
}
