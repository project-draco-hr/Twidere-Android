{
  super(context,accountId,maxId,sinceId,data,savedStatusesArgs,tabPosition);
  mQuery=query;
  mFiltersForRts=context.getSharedPreferences(SHARED_PREFERENCES_NAME,Context.MODE_PRIVATE).getBoolean(KEY_FILTERS_FOR_RTS,true);
}
