{
  try {
    final User user=showUser(twitter,id,screenName);
    if (user != null)     return user;
  }
 catch (  final TwitterException e) {
    if (!(e.getCause() instanceof IOException))     return showUserAlternative(twitter,id,screenName);
 else     throw e;
  }
  return null;
}
