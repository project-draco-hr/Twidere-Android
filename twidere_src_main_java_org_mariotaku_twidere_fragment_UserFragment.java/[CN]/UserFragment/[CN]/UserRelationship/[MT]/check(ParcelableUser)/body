{
  if (!TextUtils.equals(source,user.account_key.getId())) {
    return false;
  }
  return (user.extras != null && TextUtils.equals(target,user.extras.unique_id)) || TextUtils.equals(target,user.key.getId());
}
