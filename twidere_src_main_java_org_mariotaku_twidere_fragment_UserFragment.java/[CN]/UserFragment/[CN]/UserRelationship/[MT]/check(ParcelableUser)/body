{
  if (!account_key.equals(user.account_key)) {
    return false;
  }
  return (user.extras != null && TextUtils.equals(user_key.getId(),user.extras.unique_id)) || TextUtils.equals(user_key.getId(),user.key.getId());
}
