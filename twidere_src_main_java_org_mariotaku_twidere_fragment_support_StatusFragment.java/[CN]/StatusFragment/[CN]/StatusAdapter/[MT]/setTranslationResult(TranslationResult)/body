{
  if (mStatus == null || translation == null || TwitterContentUtils.getOriginalId(mStatus) != translation.getId()) {
    mTranslationResult=null;
  }
 else {
    mTranslationResult=translation;
  }
  notifyDataSetChanged();
}
