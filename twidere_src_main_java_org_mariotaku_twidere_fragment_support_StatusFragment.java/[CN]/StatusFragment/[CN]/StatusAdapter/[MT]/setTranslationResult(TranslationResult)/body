{
  if (mStatus == null || translation == null || InternalTwitterContentUtils.getOriginalId(mStatus) != translation.getId()) {
    mTranslationResult=null;
  }
 else {
    mTranslationResult=translation;
  }
  notifyDataSetChanged();
}
