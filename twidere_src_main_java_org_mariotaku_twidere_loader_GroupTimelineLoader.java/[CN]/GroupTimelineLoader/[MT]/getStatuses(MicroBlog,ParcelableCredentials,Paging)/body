{
  if (mGroupId != null)   return microBlog.getGroupStatuses(mGroupId,paging);
 else   if (mGroupName != null)   return microBlog.getGroupStatusesByName(mGroupName,paging);
  throw new MicroBlogException("No group name or id given");
}
