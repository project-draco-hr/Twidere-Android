{
  mDecodeQueue.clean();
  mUploadQueue.clean();
  final int n=mActiveTiles.size();
  for (int i=0; i < n; i++) {
    final Tile tile=mActiveTiles.valueAt(i);
    recycleTile(tile);
  }
  mActiveTiles.clear();
}
