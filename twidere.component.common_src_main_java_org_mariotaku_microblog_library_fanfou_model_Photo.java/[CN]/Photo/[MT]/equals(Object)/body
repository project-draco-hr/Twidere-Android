{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  Photo photo=(Photo)o;
  if (url != null ? !url.equals(photo.url) : photo.url != null)   return false;
  if (imageUrl != null ? !imageUrl.equals(photo.imageUrl) : photo.imageUrl != null)   return false;
  if (thumbUrl != null ? !thumbUrl.equals(photo.thumbUrl) : photo.thumbUrl != null)   return false;
  return largeUrl != null ? largeUrl.equals(photo.largeUrl) : photo.largeUrl == null;
}
