{
  final long[] accountIds=getAccountIds();
  final AbsActivitiesAdapter<List<ParcelableActivity>> adapter=getAdapter();
  if (adapter.getItemCount() > 0) {
    showContent();
  }
 else   if (accountIds.length > 0) {
    final ErrorInfoStore.DisplayErrorInfo errorInfo=ErrorInfoStore.getErrorInfo(getContext(),mErrorInfoStore.get(ErrorInfoStore.KEY_INTERACTIONS,accountIds[0]));
    if (errorInfo != null) {
      showError(errorInfo.getIcon(),errorInfo.getMessage());
    }
 else {
      showContent();
    }
  }
 else {
    showError(R.drawable.ic_info_accounts,getString(R.string.no_account_selected));
  }
}
