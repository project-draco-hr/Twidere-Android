{
switch (item.getItemId()) {
case R.id.save:
{
      requestAndSaveToStorage();
      return true;
    }
case R.id.refresh:
{
    startLoading();
    return true;
  }
case R.id.share:
{
  shareMedia();
}
}
return super.onOptionsItemSelected(item);
}
