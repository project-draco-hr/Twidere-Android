{
switch (item.getItemId()) {
case R.id.save:
{
      requestAndSaveToGallery();
      return true;
    }
case R.id.refresh:
{
    loadVideo(true);
    return true;
  }
}
return super.onOptionsItemSelected(item);
}
