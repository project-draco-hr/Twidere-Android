{
switch (item.getItemId()) {
case R.id.save:
{
      saveToGallery();
      return true;
    }
case R.id.refresh:
{
    loadVideo();
    return true;
  }
}
return super.onOptionsItemSelected(item);
}
