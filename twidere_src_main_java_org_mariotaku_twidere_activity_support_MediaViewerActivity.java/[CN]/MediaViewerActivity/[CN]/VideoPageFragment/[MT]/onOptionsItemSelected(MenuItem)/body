{
switch (item.getItemId()) {
case R.id.save:
{
      saveToGallery();
      return true;
    }
case R.id.refresh:
{
    loadVideo(true);
    return true;
  }
}
return super.onOptionsItemSelected(item);
}
