{
  ensureAuthorizationEnabled();
  return factory.createDirectMessageList(get(conf.getRestBaseURL() + ENDPOINT_DIRECT_MESSAGES_SENT,conf.getSigningRestBaseURL() + ENDPOINT_DIRECT_MESSAGES_SENT,INCLUDE_ENTITIES));
}
