{
  mExecutor.execute(new Runnable(){
    @Override public void run(){
      try {
        Log.d(LOGTAG,LoganSquare.serialize(event));
      }
 catch (      IOException e) {
        Log.w(LOGTAG,e);
      }
    }
  }
);
}
