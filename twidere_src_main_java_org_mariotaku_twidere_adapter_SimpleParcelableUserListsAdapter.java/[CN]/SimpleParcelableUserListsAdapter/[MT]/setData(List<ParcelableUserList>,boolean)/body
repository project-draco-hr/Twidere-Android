{
  if (clearOld) {
    clear();
  }
  if (data == null)   return;
  for (  final ParcelableUserList user : data) {
    if (clearOld || findItemPosition(user.hashCode()) < 0) {
      add(user);
    }
  }
}
