{
  final StringBuffer buf=new StringBuffer(11 * array.length);
  for (  final String str : array) {
    if (0 != buf.length()) {
      buf.append(",");
    }
    buf.append(str);
  }
  return buf.toString();
}
