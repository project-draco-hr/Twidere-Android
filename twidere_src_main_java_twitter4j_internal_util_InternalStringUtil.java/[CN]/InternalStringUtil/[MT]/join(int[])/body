{
  final StringBuffer buf=new StringBuffer(11 * array.length);
  for (  final int follow : array) {
    if (0 != buf.length()) {
      buf.append(",");
    }
    buf.append(follow);
  }
  return buf.toString();
}
