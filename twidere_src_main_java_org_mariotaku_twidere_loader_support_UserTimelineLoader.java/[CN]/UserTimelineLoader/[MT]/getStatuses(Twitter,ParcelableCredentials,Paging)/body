{
  if (mUserId != null) {
    return twitter.getUserTimeline(mUserId,paging);
  }
 else   if (mUserScreenName != null) {
    return twitter.getUserTimelineByScreenName(mUserScreenName,paging);
  }
 else   throw new TwitterException("Invalid user");
}
