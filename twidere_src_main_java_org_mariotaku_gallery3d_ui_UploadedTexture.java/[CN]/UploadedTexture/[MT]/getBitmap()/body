{
  if (mBitmap == null) {
    mBitmap=onGetBitmap();
    final int w=mBitmap.getWidth() + mBorder * 2;
    final int h=mBitmap.getHeight() + mBorder * 2;
    if (mWidth == UNSPECIFIED) {
      setSize(w,h);
    }
  }
  return mBitmap;
}
