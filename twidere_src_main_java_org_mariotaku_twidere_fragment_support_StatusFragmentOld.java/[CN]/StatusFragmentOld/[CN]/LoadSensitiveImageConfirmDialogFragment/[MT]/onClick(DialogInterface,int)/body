{
switch (which) {
case DialogInterface.BUTTON_POSITIVE:
{
      final Fragment f=getParentFragment();
      if (f instanceof StatusFragmentOld) {
        ((StatusFragmentOld)f).loadPreviewImages();
      }
      break;
    }
}
}
