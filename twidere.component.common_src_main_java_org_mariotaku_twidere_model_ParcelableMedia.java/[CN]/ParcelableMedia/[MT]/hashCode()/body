{
  return new HashCodeBuilder(17,37).append(media_url).append(url).append(preview_url).append(start).append(end).append(type).append(width).append(height).append(video_info).append(card).toHashCode();
}
