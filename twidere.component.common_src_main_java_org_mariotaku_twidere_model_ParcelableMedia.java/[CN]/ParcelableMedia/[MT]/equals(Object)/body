{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  ParcelableMedia media=(ParcelableMedia)o;
  return new EqualsBuilder().append(start,media.start).append(end,media.end).append(type,media.type).append(width,media.width).append(height,media.height).append(media_url,media.media_url).append(url,media.url).append(preview_url,media.preview_url).append(video_info,media.video_info).append(card,media.card).isEquals();
}
