{
  final Context context=getContext();
  if (context == null)   return;
  final ParcelableAccount account=mAdapter.getAccount(position);
  if (account.account_user != null) {
    IntentUtils.openUserProfile(context,account.account_user,null,true,UserFragment.Referral.SELF_PROFILE);
  }
 else {
    IntentUtils.openUserProfile(context,account.account_key,account.account_key,account.screen_name,null,true,UserFragment.Referral.SELF_PROFILE);
  }
}
